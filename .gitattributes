* text=auto eol=lf
*.png binary
*.jpg binary
*.pdf binary

# Generated by scripts/stage_lambda.py, which writes CRLF (csv.DictWriter's
# default). Without -text, git would normalise it to LF on commit and the file
# would show as modified after every staging run, forever. Store it verbatim.
backend/reference_data/brand_index.csv -text
