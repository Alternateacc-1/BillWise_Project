# BillWise backend. Python 3.12 (matches the AWS Lambda runtime ceiling).
# All packages are free and OSI-licensed.
#
# Versions below are the ones actually resolved and tested on 2026-09-18 --
# read out of the venv, never hand-written. To upgrade: install unpinned in a
# scratch venv, run the full test suite, then re-freeze.

# --- data preparation ---
pdfplumber==0.11.10        # MIT  - special-feature PDF table extraction
                           #        BUILD-TIME ONLY. Not in the Lambda bundle.

# --- matching ---
rapidfuzz==3.14.6          # MIT  - fuzzy name matching, native, no network

# --- api ---
fastapi==0.141.1           # MIT
uvicorn==0.53.0            # BSD-3 - the local dev server. Run as a command,
                           #        never imported, so it looks unused. It is not.
pydantic==2.13.5           # MIT
python-multipart==0.0.32   # Apache-2.0 - multipart upload parsing
pypdf==6.19.0              # BSD-3 - exact PDF page count before Textract
                           #        is billed. ~1 MB, pure Python, SHIPS.
mangum==0.22.0             # ISC  - FastAPI -> Lambda adapter

# --- aws adapters ---
boto3==1.43.97             # Apache-2.0

# --- demo bill generation ---
reportlab==5.0.1           # BSD-3
pillow==12.3.0             # MIT-CMU

# --- dev / test ---
pytest==9.1.1              # MIT
httpx==0.28.1              # BSD-3 - required by starlette's TestClient. Never
                           #        imported by our code; the tests need it.
ruff==0.16.8               # MIT  - linter. ruff.toml pins the rule set, so
                           #        the result is the same on every machine.
